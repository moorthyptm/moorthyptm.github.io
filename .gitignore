# Dependencies
node_modules
**/node_modules
.pnpm-store

# IDE / AI tools
.cursor
.claude
skills-lock.json

# Secrets — never commit API keys or env files
.env
.env.*
!.env.example

# Personal docs — resume, certs, scanned IDs.
# Kept out even if someone runs `git add .`.
*.pdf

# Build artefacts
**/dist
*.log

# Local-only backups
*.bak
